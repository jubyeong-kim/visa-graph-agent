# 이 파일을 .env 로 복사하고 키를 채우세요. .env 는 깃에 올라가지 않습니다.
# 어느 키가 필요한지는 config.json 의 model.provider 가 정합니다.

# provider = "anthropic" 일 때  (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# provider = "openai" 일 때  (https://platform.openai.com/api-keys)
# OPENAI_API_KEY=sk-...
